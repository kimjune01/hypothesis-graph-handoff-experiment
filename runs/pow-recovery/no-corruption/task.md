# Audit an unchanged certified DAG

All authoritative salts remain exactly as recorded in `MEMORY.md`. Verify the inherited bundle, preserve every entitled node, and rebuild nothing.

Write `answer.json` with sorted arrays `retracted`, `preserved`, and `recomputed`, plus the full six-node `receipts` mapping.
