# Agent B: continue from verifiable semantic memory

Work only from `task.md`, `MEMORY.md`, and files you create in the current directory. Do not inspect parent directories, Git history, experiment code, or other runs.

Treat `MEMORY.md` as semantic memory under the Verifiable Knowledge protocol. A standing true claim is a reusable interface, not an invitation to repeat discovery. Replay its attached cheap check when warranted by missing, stale, inconsistent, challenged, or changed roots. If a check fails, retract dependent claims and preserve independent ones.

Complete the downstream task exactly. Write the requested JSON to `answer.json`. Also write `continuation-report.md` stating which claims you inherited, which checks you replayed and why, whether you ran any discrete-log search, and what you derived anew.
