Convert the source inquiry into chronological research notes that implement `VERIFIABLE_KNOWLEDGE_PROTOCOL.md` without using typed graph nodes or explicit dependency-edge syntax.

Do not merely summarize what the previous agent said. For every consequential claim, preserve its exact bounded scope, `untrue`/`true`/`false` state, executable check, pinned inputs and environment, kill condition, last observed verdict, receipt location, replay cost, remaining trust roots, and what later conclusions would need reconsideration if it fails.

Distinguish discovery from verification. If a proof, certificate, exhaustive search, or patch already exists and its checker passes, preserve the artifact and checker so a receiver can replay verification without repeating discovery.

Write this as an ordinary chronological lab notebook: what was believed, what was tried, what failed, what became verified, and what remains next. Do not add node IDs, parent lists, graph edges, or a synthesized dependency diagram. Do not invent evidence or silently fill missing roots.
